<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>



<h1 class="heading">WHAT ON EARTH DO YOU WANT?</h1>
<div id="search-bar">
  <%= form_tag items_path, :method => 'get' do %>  
    <p>
    <div id="search-wrap">  
      <%= text_field_tag :search, params[:search], :class => "box" %>
      <%= submit_tag "SEARCH", :name => nil, :class => "search-button" %>
    </div>
    </p>  
  <% end %>
</div>




<!-- <h1>Listing items</h1>

<table>
  <thead>
    <tr>
      <th>Latitude</th>
      <th>Longitude</th>
      <th>Address</th>
      <th>Description</th>
      <th>Title</th>
      <th>Phone</th>
      <th>Price</th>
      <th>Condition</th>
      <th>Sold</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.latitude %></td>
        <td><%= item.longitude %></td>
        <td><%= item.address %></td>
        <td><%= item.description %></td>
        <td><%= item.title %></td>
        <td><%= item.phone %></td>
        <td><%= item.price %></td>
        <td><%= item.condition %></td>
        <td><%= item.sold %></td>
        <td><%= link_to 'Show', item %></td>
        <td><%= link_to 'Edit', edit_item_path(item) %></td>
        <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br> -->
<br>



<div id="map-wrap" style='width: 800px;'>
  <center><div id="map" style='width: 1000px; height: 470px;'></div></center>
</div>

<script type="text/javascript">
handler = Gmaps.build('Google');
handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
  markers = handler.addMarkers(<%=raw @hash.to_json %>);
  handler.bounds.extendWith(markers);
  handler.fitMapToBounds();
});
</script>

<div class="align-right"><%= link_to 'New Item', new_item_path %></div>